__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.coverage
htmlcov/
.venv/
venv/

# Personal photographs stay local. Analysing your own pictures is the point of
# this tool; committing them is not.
samples/

# IDE
.idea/
.vscode/

# pyenv-virtualenv name, specific to one machine.
.python-version

# Bot secrets, written by the deploy workflow. Never committed.
deploy/.env

# Personal notes. These hold real server addresses and ports, so they must
# never reach a public repository.
*.local.md
